#include <stdio.h>

int main() {

    const int NUM = 5;
    printf("%d\n", NUM);

    int const NUM2 = 7;
    printf("%d\n", NUM2);

    // const make a variable immutable
    // we can use const after or before declaring data type
    // in practice constant variable names are written in capital letters
    


    return 0;
}